name: Github demo
on: push
jobs: 
  Example-job:
    name: exploring github actions
    runs-on: ubuntu-latest
    steps: 
     - run: echo "the jos was triggered by a ${{github.event_name}}"
     - run: echo "OS ... ${{ runner.os }}"
 
